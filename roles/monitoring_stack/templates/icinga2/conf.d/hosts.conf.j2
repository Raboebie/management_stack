/* Host definition — generated by Ansible
 * Points checks at the Docker host rather than container localhost.
 */

object Host NodeName {
  import "generic-host"
  address = "{{ icinga2_host_address | default('172.17.0.1') }}"
  vars.os = "Linux"

  vars.http_vhosts["Grafana"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = {{ grafana_port }}
  }
  vars.http_vhosts["IcingaWeb2"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = {{ icingaweb2_port }}
  }
  vars.http_vhosts["InfluxDB"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/health"
    http_port = {{ influxdb_port }}
  }
  vars.http_vhosts["Portainer"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = 9000
  }
  vars.http_vhosts["Semaphore"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = 3001
  }
  vars.http_vhosts["Ollama"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/api/tags"
    http_port = {{ ollama_port }}
  }
  vars.http_vhosts["OpenWebUI"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = {{ open_webui_port }}
  }
  vars.http_vhosts["Tabby"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/"
    http_port = {{ tabby_port }}
  }
  vars.http_vhosts["k3s API"] = {
    http_address = "{{ icinga2_host_address | default('172.17.0.1') }}"
    http_uri = "/readyz"
    http_port = {{ k3s_api_port }}
    http_ssl = true
    http_expect = "401 Unauthorized"
  }
}
